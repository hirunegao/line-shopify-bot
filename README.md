# LINE Shopify Bot

This project is a LINE bot that connects to Shopify, Notion and OpenAI. Environment variables are required for the services to work.

1. Copy `.env.example` to `.env`.
2. Fill in each variable with your own credentials.
3. Run the bot using `npm start`.

